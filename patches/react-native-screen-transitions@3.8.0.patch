diff --git a/node_modules/react-native-screen-transitions/.bun-tag-b4886d09fec6e293 b/.bun-tag-b4886d09fec6e293
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/lib/commonjs/shared/utils/bounds/navigation/zoom/build.js b/lib/commonjs/shared/utils/bounds/navigation/zoom/build.js
index 7f2663c83a194f7aae1d84746c19cea7f433c20e..50a4a231061cdfd26e6bdd745d447cbf1870dd0b 100644
--- a/lib/commonjs/shared/utils/bounds/navigation/zoom/build.js
+++ b/lib/commonjs/shared/utils/bounds/navigation/zoom/build.js
@@ -303,9 +303,13 @@ function buildZoomStyles({
   const elementGestureX = shouldTrackGestureTranslation ? dragX : 0;
   const elementGestureY = shouldTrackGestureTranslation ? dragY : 0;
   const safeUnfocusedContentScale = Math.max(Math.abs(unfocusedContentScale), _constants.EPSILON);
-  // A naturally settled close can leave this as the last emitted style frame,
-  // so drop temporary stacking here instead of waiting for a later reset pass.
-  const shouldElevateUnfocusedElement = !props.active.closing || !props.active.settled;
+  // Keep the source element stacked above its siblings for the WHOLE transition,
+  // including the entire close. The original `!closing || !settled` dropped the
+  // zIndex the instant `settled` flipped, which happens while the element is
+  // still visually shrinking back into the grid — so the closing icon fell
+  // behind its neighbours. `closing` keeps it elevated until the close fully
+  // ends, after which the screen's style map resets the value anyway.
+  const shouldElevateUnfocusedElement = !props.active.settled || props.active.closing;
   const scaleShiftX = (0, _math.computeCenterScaleShift)({
     center: elementCenterX,
     containerCenter: screenLayout.width / 2,
@@ -332,7 +336,20 @@ function buildZoomStyles({
   const elementTranslateY = (0, _helpers.toNumber)(elementRaw.translateY) + compensatedGestureY;
   const elementScaleX = (0, _helpers.toNumber)(elementRaw.scaleX, 1) * elementGestureScale / safeUnfocusedContentScale;
   const elementScaleY = (0, _helpers.toNumber)(elementRaw.scaleY, 1) * elementGestureScale / safeUnfocusedContentScale;
-  const resolvedElementStyle = shouldHideUnfocusedElement ? {
+  const resolvedElementStyle = props.active.settled ? {
+    transform: [{
+      translateX: 0
+    }, {
+      translateY: 0
+    }, {
+      scaleX: 1
+    }, {
+      scaleY: 1
+    }],
+    opacity: 1,
+    zIndex: 0,
+    elevation: 0
+  } : shouldHideUnfocusedElement ? {
     transform: [{
       translateX: 0
     }, {
diff --git a/lib/module/shared/utils/bounds/navigation/zoom/build.js b/lib/module/shared/utils/bounds/navigation/zoom/build.js
index d9829be607730020335bda94c08d55a43fa38f12..f84f2e20e4164bf2f267d29955fb06b51368299b 100644
--- a/lib/module/shared/utils/bounds/navigation/zoom/build.js
+++ b/lib/module/shared/utils/bounds/navigation/zoom/build.js
@@ -299,9 +299,13 @@ export function buildZoomStyles({
   const elementGestureX = shouldTrackGestureTranslation ? dragX : 0;
   const elementGestureY = shouldTrackGestureTranslation ? dragY : 0;
   const safeUnfocusedContentScale = Math.max(Math.abs(unfocusedContentScale), EPSILON);
-  // A naturally settled close can leave this as the last emitted style frame,
-  // so drop temporary stacking here instead of waiting for a later reset pass.
-  const shouldElevateUnfocusedElement = !props.active.closing || !props.active.settled;
+  // Keep the source element stacked above its siblings for the WHOLE transition,
+  // including the entire close. The original `!closing || !settled` dropped the
+  // zIndex the instant `settled` flipped, which happens while the element is
+  // still visually shrinking back into the grid — so the closing icon fell
+  // behind its neighbours. `closing` keeps it elevated until the close fully
+  // ends, after which the screen's style map resets the value anyway.
+  const shouldElevateUnfocusedElement = !props.active.settled || props.active.closing;
   const scaleShiftX = computeCenterScaleShift({
     center: elementCenterX,
     containerCenter: screenLayout.width / 2,
@@ -328,7 +332,20 @@ export function buildZoomStyles({
   const elementTranslateY = toNumber(elementRaw.translateY) + compensatedGestureY;
   const elementScaleX = toNumber(elementRaw.scaleX, 1) * elementGestureScale / safeUnfocusedContentScale;
   const elementScaleY = toNumber(elementRaw.scaleY, 1) * elementGestureScale / safeUnfocusedContentScale;
-  const resolvedElementStyle = shouldHideUnfocusedElement ? {
+  const resolvedElementStyle = props.active.settled ? {
+    transform: [{
+      translateX: 0
+    }, {
+      translateY: 0
+    }, {
+      scaleX: 1
+    }, {
+      scaleY: 1
+    }],
+    opacity: 1,
+    zIndex: 0,
+    elevation: 0
+  } : shouldHideUnfocusedElement ? {
     transform: [{
       translateX: 0
     }, {
diff --git a/src/shared/utils/bounds/navigation/zoom/build.ts b/src/shared/utils/bounds/navigation/zoom/build.ts
index c1eed4dc12f227aebf3d65d0d2efbb88154c1d7e..ebecbe99b0661695661fcbaac70c1213215cd73c 100644
--- a/src/shared/utils/bounds/navigation/zoom/build.ts
+++ b/src/shared/utils/bounds/navigation/zoom/build.ts
@@ -408,10 +408,14 @@ export function buildZoomStyles({
 		Math.abs(unfocusedContentScale),
 		EPSILON,
 	);
-	// A naturally settled close can leave this as the last emitted style frame,
-	// so drop temporary stacking here instead of waiting for a later reset pass.
+	// Keep the source element stacked above its siblings for the WHOLE transition,
+	// including the entire close. The original `!closing || !settled` dropped the
+	// zIndex the instant `settled` flipped, which happens while the element is
+	// still visually shrinking back into the grid — so the closing icon fell
+	// behind its neighbours. `closing` keeps it elevated until the close fully
+	// ends, after which the screen's style map resets the value anyway.
 	const shouldElevateUnfocusedElement =
-		!props.active.closing || !props.active.settled;
+		!props.active.settled || props.active.closing;
 
 	const scaleShiftX = computeCenterScaleShift({
 		center: elementCenterX,
@@ -448,7 +452,24 @@ export function buildZoomStyles({
 		(toNumber(elementRaw.scaleY, 1) * elementGestureScale) /
 		safeUnfocusedContentScale;
 
-	const resolvedElementStyle = shouldHideUnfocusedElement
+	// A settled transition means the source element is at rest by definition, so
+	// pin it to its natural state (identity transform, visible, no stacking). The
+	// last emitted frame of a settled close can otherwise carry a glitched
+	// (enlarged) bounds transform that briefly flashes over the grid before the
+	// screen unmounts. Forcing rest here fixes that flash without hiding the icon.
+	const resolvedElementStyle = props.active.settled
+		? {
+				transform: [
+					{ translateX: 0 },
+					{ translateY: 0 },
+					{ scaleX: 1 },
+					{ scaleY: 1 },
+				],
+				opacity: 1,
+				zIndex: 0,
+				elevation: 0,
+			}
+		: shouldHideUnfocusedElement
 		? {
 				transform: [
 					{ translateX: 0 },
