diff --git a/lib/module/components/ChoreographyScreenBase.js b/lib/module/components/ChoreographyScreenBase.js
index a89dcffa3fdfc7177ab36fa2f845d0c892c30cf5..15d23a8307f8003673b9c95c3da27481a7f206a1 100644
--- a/lib/module/components/ChoreographyScreenBase.js
+++ b/lib/module/components/ChoreographyScreenBase.js
@@ -14,7 +14,9 @@ export function ChoreographyScreenBase({
   instanceId,
   isFocused = true,
   children,
-  ready = true
+  ready = true,
+  keepVisible = false,
+  interactiveWhileReturning = false
 }) {
   const screenId = instanceId ?? screenName;
   const choreography = useContext(ChoreographyContext);
@@ -35,10 +37,11 @@ export function ChoreographyScreenBase({
   const revealStyle = useAnimatedStyle(() => {
     const value = progress?.value ?? 0;
     return {
-      opacity: deriveScreenOpacity(direction, role, phase, value)
+      opacity: keepVisible ? 1 : deriveScreenOpacity(direction, role, phase, value)
     };
-  }, [direction, role, phase, progress]);
-  const blockInteraction = isPendingTarget || shouldBlockInteraction(role, phase);
+  }, [direction, role, phase, progress, keepVisible]);
+  const returning = interactiveWhileReturning && role === 'target' && direction === 'backward';
+  const blockInteraction = !returning && (isPendingTarget || shouldBlockInteraction(role, phase));
   const interactionOwner = choreography?.interactionOwner;
   const interactionProps = useAnimatedProps(() => ({
     pointerEvents: blockInteraction && interactionOwner?.value !== screenId ? 'none' : 'auto'
@@ -93,7 +96,7 @@ export function ChoreographyScreenBase({
       style: [styles.container, {
         opacity: staticOpacity
       }],
-      pointerEvents: isPendingTarget || role !== 'inactive' && phase === 'preparing' ? 'none' : 'box-none',
+      pointerEvents: !returning && (isPendingTarget || role !== 'inactive' && phase === 'preparing') ? 'none' : 'box-none',
       children: /*#__PURE__*/_jsx(Animated.View, {
         style: [styles.container, revealStyle],
         pointerEvents: blockInteraction ? 'none' : 'auto',
diff --git a/lib/module/core/ProgressOwnership.js b/lib/module/core/ProgressOwnership.js
index bc513dccacc33d22f1eb9bd0feb8338478b6fa1d..6459c529631467b930da360af13867d1b66feee0 100644
--- a/lib/module/core/ProgressOwnership.js
+++ b/lib/module/core/ProgressOwnership.js
@@ -3,6 +3,20 @@
 import { cancelAnimation, Easing, withSpring, withTiming } from 'react-native-reanimated';
 import { scheduleOnRN, scheduleOnUI } from 'react-native-worklets';
 import { finishVisibilityHandoff, resumeVisibilityHandoff } from "./ElementVisibilityRegistry.js";
+
+// Completion callbacks reach the UI thread as remote functions, which
+// worklets resolves through a WEAK registry on the JS side. A callback
+// replaced by a re-render while its spring is still running is collected
+// before the spring finishes; the scheduler then resolves nothing and the
+// process aborts (jsi getObject assertion in scheduleOnRN). So the callbacks
+// are held HERE, strongly, keyed by their animation token, and the worklet
+// schedules one module-level function that never goes away.
+const completions = new Map();
+function dispatchCompletion(token, sessionId) {
+  const onComplete = completions.get(token);
+  completions.delete(token);
+  onComplete?.(token, sessionId);
+}
 export class ProgressOwnership {
   generation = 0;
   sessionId = null;
@@ -23,6 +37,8 @@ export class ProgressOwnership {
     this.invalidate();
   }
   invalidate() {
+    // Every older token is stale now; drop the callbacks waiting on them.
+    completions.clear();
     const token = ++this.generation;
     const {
       owner,
@@ -65,14 +81,16 @@ export function setOwnedProgress(ownership, token, sessionId, progress, value, o
     owner,
     handoff
   } = ownership;
+  const hasCompletion = Boolean(onComplete);
+  if (onComplete) completions.set(token, onComplete);
   scheduleOnUI(() => {
     'worklet';
 
     if (owner.value !== token) return;
     progress.value = value;
-    if (onComplete) {
+    if (hasCompletion) {
       if (value === 0 || value === 1) finishVisibilityHandoff(handoff, sessionId);
-      scheduleOnRN(onComplete, token, sessionId);
+      scheduleOnRN(dispatchCompletion, token, sessionId);
     }
   });
 }
@@ -93,6 +111,7 @@ export function animateOwnedProgress({
     owner,
     handoff
   } = ownership;
+  completions.set(token, onComplete);
   scheduleOnUI(() => {
     'worklet';
 
@@ -105,7 +124,7 @@ export function animateOwnedProgress({
           finishVisibilityHandoff(handoff, sessionId);
         }
         onCompleteUI?.();
-        scheduleOnRN(onComplete, token, sessionId);
+        scheduleOnRN(dispatchCompletion, token, sessionId);
       }
     };
     progress.value = duration ? withTiming(target, {
diff --git a/lib/typescript/src/components/ChoreographyScreenBase.d.ts b/lib/typescript/src/components/ChoreographyScreenBase.d.ts
index 625d720f6bf992d17d32678bf9d307530aea7766..549025f5bb9ff68651d6ca238b26285e1a91db6f 100644
--- a/lib/typescript/src/components/ChoreographyScreenBase.d.ts
+++ b/lib/typescript/src/components/ChoreographyScreenBase.d.ts
@@ -4,6 +4,10 @@ export interface ChoreographyScreenProps {
     children: React.ReactNode;
     /** Additional app readiness gate applied after the screen has laid out. */
     ready?: boolean;
+    /** Keep this screen fully visible during a session instead of cross-fading it with the other endpoint. */
+    keepVisible?: boolean;
+    /** Keep this screen touchable while a close flies back onto it. */
+    interactiveWhileReturning?: boolean;
 }
 export declare function ChoreographyScreenBase({ screenId: screenName, instanceId, isFocused, children, ready, }: ChoreographyScreenProps & {
     instanceId?: string;
diff --git a/src/components/ChoreographyScreenBase.tsx b/src/components/ChoreographyScreenBase.tsx
index 1313eab88f325359479c6e2532aa710a919b2067..78dc1960a63552ca82f5b52ec7fdbf01f2dd2064 100644
--- a/src/components/ChoreographyScreenBase.tsx
+++ b/src/components/ChoreographyScreenBase.tsx
@@ -23,6 +23,18 @@ export interface ChoreographyScreenProps {
   children: React.ReactNode;
   /** Additional app readiness gate applied after the screen has laid out. */
   ready?: boolean;
+  /**
+   * Keep this screen fully visible during a session instead of cross-fading
+   * it with the other endpoint. For a transparent destination (a preview
+   * over the list it came from) the source must stay on screen.
+   */
+  keepVisible?: boolean;
+  /**
+   * Keep this screen touchable while a close flies back onto it. A tap then
+   * reaches it mid-flight, and a navigation it starts is queued until the
+   * close lands, instead of the touch being dropped.
+   */
+  interactiveWhileReturning?: boolean;
 }
 
 export function ChoreographyScreenBase({
@@ -31,6 +43,8 @@ export function ChoreographyScreenBase({
   isFocused = true,
   children,
   ready = true,
+  keepVisible = false,
+  interactiveWhileReturning = false,
 }: ChoreographyScreenProps & { instanceId?: string; isFocused?: boolean }) {
   const screenId = instanceId ?? screenName;
   const choreography = useContext(ChoreographyContext);
@@ -65,11 +79,17 @@ export function ChoreographyScreenBase({
 
   const revealStyle = useAnimatedStyle(() => {
     const value = progress?.value ?? 0;
-    return { opacity: deriveScreenOpacity(direction, role, phase, value) };
-  }, [direction, role, phase, progress]);
+    return {
+      opacity: keepVisible
+        ? 1
+        : deriveScreenOpacity(direction, role, phase, value),
+    };
+  }, [direction, role, phase, progress, keepVisible]);
 
+  const returning =
+    interactiveWhileReturning && role === 'target' && direction === 'backward';
   const blockInteraction =
-    isPendingTarget || shouldBlockInteraction(role, phase);
+    !returning && (isPendingTarget || shouldBlockInteraction(role, phase));
   const interactionOwner = choreography?.interactionOwner;
   const interactionProps = useAnimatedProps(() => ({
     pointerEvents:
@@ -138,7 +158,8 @@ export function ChoreographyScreenBase({
         onLayout={handleLayout}
         style={[styles.container, { opacity: staticOpacity }]}
         pointerEvents={
-          isPendingTarget || (role !== 'inactive' && phase === 'preparing')
+          !returning &&
+          (isPendingTarget || (role !== 'inactive' && phase === 'preparing'))
             ? 'none'
             : 'box-none'
         }
diff --git a/src/core/ProgressOwnership.ts b/src/core/ProgressOwnership.ts
index 9e72c7c89dde625dac72f0589b3674f311f84212..f24ce6d24f9a6995afeec9b815d8eb35ecc0c85f 100644
--- a/src/core/ProgressOwnership.ts
+++ b/src/core/ProgressOwnership.ts
@@ -13,6 +13,21 @@ import {
   type VisibilityHandoff,
 } from './ElementVisibilityRegistry';
 
+// Completion callbacks reach the UI thread as remote functions, which
+// worklets resolves through a WEAK registry on the JS side. A callback
+// replaced by a re-render while its spring is still running is collected
+// before the spring finishes; the scheduler then resolves nothing and the
+// process aborts (jsi getObject assertion in scheduleOnRN). So the callbacks
+// are held HERE, strongly, keyed by their animation token, and the worklet
+// schedules one module-level function that never goes away.
+type Completion = (token: number, sessionId: string) => void;
+const completions = new Map<number, Completion>();
+function dispatchCompletion(token: number, sessionId: string): void {
+  const onComplete = completions.get(token);
+  completions.delete(token);
+  onComplete?.(token, sessionId);
+}
+
 export class ProgressOwnership {
   private generation = 0;
   private sessionId: string | null = null;
@@ -38,6 +53,8 @@ export class ProgressOwnership {
   }
 
   invalidate(): void {
+    // Every older token is stale now; drop the callbacks waiting on them.
+    completions.clear();
     const token = ++this.generation;
     const { owner, progress } = this;
     scheduleOnUI(() => {
@@ -80,14 +97,15 @@ export function setOwnedProgress(
 ): void {
   if (!ownership.isCurrent(token, sessionId)) return;
   const { owner, handoff } = ownership;
+  const hasCompletion = Boolean(onComplete);
+  if (onComplete) completions.set(token, onComplete);
   scheduleOnUI(() => {
     'worklet';
     if (owner.value !== token) return;
     progress.value = value;
-    if (onComplete) {
-      if (value === 0 || value === 1)
-        finishVisibilityHandoff(handoff, sessionId);
-      scheduleOnRN(onComplete, token, sessionId);
+    if (hasCompletion) {
+      if (value === 0 || value === 1) finishVisibilityHandoff(handoff, sessionId);
+      scheduleOnRN(dispatchCompletion, token, sessionId);
     }
   });
 }
@@ -119,6 +137,7 @@ export function animateOwnedProgress({
 }): void {
   if (!ownership.isCurrent(token, sessionId)) return;
   const { owner, handoff } = ownership;
+  completions.set(token, onComplete);
   scheduleOnUI(() => {
     'worklet';
     if (owner.value !== token) return;
@@ -129,7 +148,7 @@ export function animateOwnedProgress({
           finishVisibilityHandoff(handoff, sessionId);
         }
         onCompleteUI?.();
-        scheduleOnRN(onComplete, token, sessionId);
+        scheduleOnRN(dispatchCompletion, token, sessionId);
       }
     };
     progress.value = duration
