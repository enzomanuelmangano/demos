# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# clock-time-picker: A centered card with an analog Skia clock on the left and a vertical snap-scrolling time-slot list on the right; scrolling the list animates the clock hands. Tests driving the clock by swiping the time list vertically.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/clock-time-picker"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/clock-time-picker--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "62%,58%"
    end: "62%,40%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "62%,58%"
    end: "62%,40%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "62%,42%"
    end: "62%,58%"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/clock-time-picker--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
