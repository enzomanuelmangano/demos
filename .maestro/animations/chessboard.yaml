# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# chessboard: Full-width react-native-chessboard (white at bottom, vertically centered) with an auto-replay plus drag-to-move pieces. Interaction tested: drag the e-file white pawn forward two squares (e2->e4 style move).
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/chessboard"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/chessboard--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "56%,64%"
    end: "56%,53%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/chessboard--pawn-moved"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/chessboard--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
