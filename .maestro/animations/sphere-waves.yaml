# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# sphere-waves: Skia fibonacci-sphere particle field with continuous color cycling; tapping the right half generates a new random pattern (magicalMul), tapping the left half steps back through history. Interaction tested: tap right to mutate, tap left to undo.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/sphere-waves"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/sphere-waves--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "80%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    point: "80%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/sphere-waves--mutated"
- tapOn:
    point: "20%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/sphere-waves--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
