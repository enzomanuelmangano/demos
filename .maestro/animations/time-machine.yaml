# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# time-machine: Full-screen 'time machine' surface; swipe down to open the curved history stack revealing the scrollable cards behind it.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/time-machine"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/time-machine--mounted"
# this demo raises a native alert (outside the RN view tree)
- tapOn:
    text: "OK"
    optional: true
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "time-machine-surface"
    direction: "DOWN"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    from:
      id: "time-machine-surface"
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "moved"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/time-machine--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
