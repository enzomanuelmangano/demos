# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# blur-cards: Skia radial-gradient scene with 5 stacked frosted cards; a full-screen PressableOpacity toggles a spring that fans the cards out and blurs them in/out. Interaction tested: tap to fan out, tap again to collapse.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/blur-cards"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/blur-cards--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/blur-cards--fanned"
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/blur-cards--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
