# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# staggered-card-number: Centered credit-card row showing a 16-digit number with an eye toggle button on the card's right edge; toggling staggers the first 12 digits between hidden and revealed. Tests tapping the eye button to reveal then hide.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/staggered-card-number"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/staggered-card-number--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "85%,50%"
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    point: "85%,50%"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/staggered-card-number--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
