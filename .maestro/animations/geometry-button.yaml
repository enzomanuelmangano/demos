# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# geometry-button: Skia geometry button (concentric circles) centered on black screen; tapping/holding morphs the stroke width with a spring + haptic. Test: tap the button to trigger the active-state animation.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/geometry-button"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/geometry-button--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/geometry-button--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
