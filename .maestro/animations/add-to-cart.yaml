# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# add-to-cart: Product list where tapping a row's buy button morphs it into a confirm bottom-sheet; tap an item to open, confirm 'Add to cart', then dismiss.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/add-to-cart"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/add-to-cart--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "add-to-cart-buy-0"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "add-to-cart-confirm"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/add-to-cart--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
