# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# scrollable-shapes: 3D point-cloud that morphs between shapes, driven by an invisible full-screen horizontal paged ScrollView (snapToInterval = page width). Interaction tested: horizontal swipes to page through and morph the shapes.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/scrollable-shapes"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/scrollable-shapes--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/scrollable-shapes--page2"
- swipe:
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/scrollable-shapes--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
