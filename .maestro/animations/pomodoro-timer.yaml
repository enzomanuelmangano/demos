# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# pomodoro-timer: Circular draggable ruler slider that sets a count (with haptics), plus a start/stop button bottom-right.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/pomodoro-timer"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/pomodoro-timer--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "pomodoro-timer-slider"
    direction: "RIGHT"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    from:
      id: "pomodoro-timer-slider"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    id: "pomodoro-timer-start"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/pomodoro-timer--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
