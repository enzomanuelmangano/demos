# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# scroll-progress: Vertical sectioned content list with a scroll-driven progress indicator; scroll DOWN through sections then back UP, then ASSERT the scroll drove the progress (scroll-progress-status latches to "scrolled").
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/scroll-progress"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/scroll-progress--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- scroll:
    direction: "DOWN"
- scroll:
    direction: "DOWN"
- waitForAnimationToEnd:
    timeout: 2000
- scroll:
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "scrolled"
    timeout: 4000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/scroll-progress--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
