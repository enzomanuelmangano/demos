# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# liquid-glass-playground: A full-screen liquid-glass overlay over a photo that follows a vertical drag with friction and progressive haptics, then springs back to center on release (horizontal drags are rejected via failOffsetX). Tests dragging the glass vertically.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/liquid-glass-playground"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/liquid-glass-playground--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "50%,40%"
    end: "50%,75%"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    start: "50%,60%"
    end: "50%,25%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/liquid-glass-playground--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
