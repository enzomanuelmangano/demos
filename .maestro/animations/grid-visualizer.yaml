# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# grid-visualizer: Skia dot-grid that renders a number; tapping the grid clears it then renders a new random number (700ms delayed).
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/grid-visualizer"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/grid-visualizer--mounted"
# this demo raises a native alert (outside the RN view tree)
- tapOn:
    text: "OK"
    optional: true
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "grid-visualizer"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "grid-visualizer"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "renders:2"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/grid-visualizer--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
