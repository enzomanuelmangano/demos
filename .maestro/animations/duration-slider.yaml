# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# duration-slider: Circular (donut) Skia slider centered on screen; the picker handle starts at the top of the donut and is dragged around the ring to change the 1..12 value. Tested by dragging the handle along the ring arc.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/duration-slider"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/duration-slider--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "50%,30%"
    end: "72%,42%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "72%,42%"
    end: "70%,62%"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/duration-slider--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
