# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# bezier-curve-outline: A Skia demo that animates a stroke tracing a bezier-curve outline; an 'Outline Mode' button toggles the outline overlay. Tap the toggle to switch modes.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/bezier-curve-outline"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/bezier-curve-outline--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    id: "bezier-curve-outline-toggle"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/bezier-curve-outline--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
