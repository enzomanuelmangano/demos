# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# threads-holo-ticket: A centered holographic ticket; tapping it flips it front/back (the holo tilt is device-motion driven). Tap the ticket to flip it.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/threads-holo-ticket"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/threads-holo-ticket--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "threads-holo-ticket"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "threads-holo-ticket"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "flipped"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/threads-holo-ticket--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
