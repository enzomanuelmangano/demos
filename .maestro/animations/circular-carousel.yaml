# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# circular-carousel: Swipe the carousel to page through, then toggle the Scale switch on; ASSERTS the toggle outcome (circular-carousel-status reads "scale:on").
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/circular-carousel"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/circular-carousel--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "circular-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    from:
      id: "circular-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "circular-carousel-switch"
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "scale:on"
    timeout: 4000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/circular-carousel--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
