# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# radar-chart: Skia radar chart with a 'Shuffle' button that re-randomizes the data and animates the polygons. Tap Shuffle to trigger the animation.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/radar-chart"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/radar-chart--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    text: "Shuffle"
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    text: "Shuffle"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/radar-chart--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
