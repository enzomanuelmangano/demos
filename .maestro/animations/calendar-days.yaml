# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# calendar-days: Flip-clock style calendar card driven by a custom AnimatedSlider at the bottom; dragging the slider flips through 31 day pages with haptics. Interaction tested: drag the slider across its track to flip days.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/calendar-days"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/calendar-days--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "12%,82%"
    end: "88%,82%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/calendar-days--end"
- swipe:
    start: "60%,82%"
    end: "30%,82%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/calendar-days--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
