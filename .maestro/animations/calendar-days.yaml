# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# calendar-days: Page-flip 'tear-off' calendar driven by a draggable slider (pan gesture); dragging the slider RIGHT flips through the days while the card animates each page. Note: there is NO tappable day grid — the only control is the slider, so a day-tap is impossible; we swipe the slider track instead.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/calendar-days"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/calendar-days--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "calendar-days-slider"
    direction: "RIGHT"
- waitForAnimationToEnd:
    timeout: 2500
- swipe:
    from:
      id: "calendar-days-slider"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 2500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/calendar-days--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
