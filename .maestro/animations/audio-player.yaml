# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# audio-player: Audio waveform scrubber (centered, ~80px tall) with a pan-draggable playhead and a live time readout. Drag across the waveform to scrub.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/audio-player"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/audio-player--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "10%,50%"
    end: "90%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- swipe:
    start: "90%,50%"
    end: "40%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/audio-player--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
