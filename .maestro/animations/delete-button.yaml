# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# delete-button: A centered Skia 'Delete' button with a gooey morph; first tap morphs to 'Confirm', second tap confirms. Tap the button twice.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/delete-button"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/delete-button--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "delete-button"
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    id: "delete-button"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "deleted"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/delete-button--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
