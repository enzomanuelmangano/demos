# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# empty-qr-code: A centered QR code that assembles/disassembles its modules with a staggered animation each time it is pressed. Taps to toggle assemble then disassemble, and ASSERTS it actually assembled (empty-qr-code-status latches "assembled").
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/empty-qr-code"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/empty-qr-code--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "empty-qr-code-toggle"
- waitForAnimationToEnd:
    timeout: 1800
- tapOn:
    id: "empty-qr-code-toggle"
- waitForAnimationToEnd:
    timeout: 1800
- assertVisible:
    text: "assembled"
    timeout: 4000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/empty-qr-code--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
