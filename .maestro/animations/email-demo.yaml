# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# email-demo: Email-style stacked list with two floating action buttons at the bottom (green restore, red delete) that animate cards in/out and shake at the limits. Tests pressing delete several times then restore.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/email-demo"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/email-demo--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "email-demo-delete"
- waitForAnimationToEnd:
    timeout: 1200
- tapOn:
    id: "email-demo-delete"
- waitForAnimationToEnd:
    timeout: 1200
- tapOn:
    id: "email-demo-restore"
- waitForAnimationToEnd:
    timeout: 1200
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/email-demo--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
