# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# image-cropper: Skia image cropper with draggable grid corners and Expand/Collapse/Crop buttons. Drag a corner to resize the crop region, then tap Crop to open the cropped detail modal.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/image-cropper"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/image-cropper--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    text: "Collapse"
- waitForAnimationToEnd:
    timeout: 3000
- swipe:
    start: "30%,30%"
    end: "50%,45%"
- tapOn:
    text: "Crop"
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    text: "Back"
    optional: true
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/image-cropper--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
