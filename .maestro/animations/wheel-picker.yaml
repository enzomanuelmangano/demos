# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# wheel-picker: Animated count display driven by a vertical draggable ruler/wheel in the center; a bottom-right button toggles expanded (wider) line spacing. Tests vertical swipes on the wheel to change the value, then tapping expand.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/wheel-picker"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/wheel-picker--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "wheel-picker-wheel"
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1200
- swipe:
    from:
      id: "wheel-picker-wheel"
    direction: "DOWN"
- tapOn:
    id: "wheel-picker-expand"
- waitForAnimationToEnd:
    timeout: 1200
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/wheel-picker--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
