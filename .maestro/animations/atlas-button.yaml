# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# atlas-button: Skia Atlas button ('React Native' label) centered; tapping animates the square atlas + scales the button with a spring.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/atlas-button"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/atlas-button--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "atlas-button"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "atlas-button"
- waitForAnimationToEnd:
    timeout: 2000
- takeScreenshot: ".maestro/artifacts/atlas-button--pressed"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/atlas-button--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
