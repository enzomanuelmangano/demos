# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# magnet-spring: A draggable graphite 'cheese' puck on a field of fibonacci-placed magnets; dragging it snaps to the nearest magnet with a metallic spring and magnets scale/recolor as it nears. Tests dragging the centered puck across the field so it springs to a magnet.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/magnet-spring"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/magnet-spring--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "50%,50%"
    end: "75%,30%"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    start: "75%,30%"
    end: "30%,70%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/magnet-spring--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
