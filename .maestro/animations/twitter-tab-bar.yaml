# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# twitter-tab-bar: Twitter-style bottom tab bar (Home, Search, Notifications, Message) with an animated active indicator and a contextual floating button. Tap different tabs to move the indicator.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/twitter-tab-bar"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/twitter-tab-bar--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "38%,94%"
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    point: "88%,94%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/twitter-tab-bar--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
