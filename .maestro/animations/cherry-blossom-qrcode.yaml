# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# cherry-blossom-qrcode: WebGPU cherry-blossom tree whose canopy encodes a QR code; a TextInput (auto-focused) drives the QR content live. We type a URL into the input, then tap the canvas to toggle the flat/3D view.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/cherry-blossom-qrcode"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/cherry-blossom-qrcode--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "cherry-blossom-qrcode-input"
- inputText: "hello"
- waitForAnimationToEnd:
    timeout: 2500
- tapOn:
    id: "cherry-blossom-qrcode-canvas"
- waitForAnimationToEnd:
    timeout: 2500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/cherry-blossom-qrcode--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
