# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# verification-code-face: An OTP code-entry screen with an animated face that reacts to typing; tapping the code area focuses a hidden TextInput and opens the number pad, and entering the code (1111) drives the face animation. Tested by focusing the field and typing the code.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/verification-code-face"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/verification-code-face--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,38%"
- waitForAnimationToEnd:
    timeout: 1000
- inputText: "1111"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/verification-code-face--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
