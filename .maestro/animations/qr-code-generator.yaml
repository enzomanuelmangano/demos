# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# qr-code-generator: QR generator (qrcode demo): a TextInput at top drives a live Skia QR code, plus a 'Deviation' slider. Type new text to regenerate the QR, then drag the slider to change the discrete-path deviation.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/qr-code-generator"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/qr-code-generator--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,12%"
- inputText: "reactiive"
- waitForAnimationToEnd:
    timeout: 3000
- swipe:
    start: "15%,90%"
    end: "80%,90%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/qr-code-generator--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
