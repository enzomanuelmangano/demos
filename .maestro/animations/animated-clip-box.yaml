# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# animated-clip-box: Two cards with a clip-path reveal on press; pressing 'Explore Demos' then 'Learn More' triggers the expanding-circle clip animation revealing the description.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/animated-clip-box"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/animated-clip-box--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "animated-clip-box-explore"
- waitForAnimationToEnd:
    timeout: 2000
- tapOn:
    id: "animated-clip-box-learn-more"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/animated-clip-box--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
