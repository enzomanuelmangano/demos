# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# expandable-mini-player: A bottom tab bar with a mini player docked just above it; tapping the mini player expands it into a full sheet (tap gesture), and swiping/panning down collapses it. Tested by tapping to expand then swiping down to collapse.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/expandable-mini-player"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/expandable-mini-player--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,85%"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    start: "50%,25%"
    end: "50%,85%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/expandable-mini-player--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
