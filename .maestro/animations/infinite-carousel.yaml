# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# infinite-carousel: Infinite circular horizontal carousel of theme cards with snap + a background gradient that animates to the active card's color. Tests horizontal paged swipes that advance the active card.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/infinite-carousel"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/infinite-carousel--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "infinite-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 1200
- swipe:
    from:
      id: "infinite-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 1200
- swipe:
    from:
      id: "infinite-carousel"
    direction: "RIGHT"
- waitForAnimationToEnd:
    timeout: 1200
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/infinite-carousel--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
