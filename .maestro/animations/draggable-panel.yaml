# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# draggable-panel: A floating 64x64 settings button (bottom-right) that, when dragged, springs and snaps to the nearest screen corner. Tests dragging the floating control from its bottom-right home to the top-left corner so it snaps.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/draggable-panel"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/draggable-panel--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "87%,86%"
    end: "15%,18%"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    start: "15%,18%"
    end: "87%,86%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/draggable-panel--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
