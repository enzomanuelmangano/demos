# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# split-button: A single centered 'Start' chip that splits into two icon buttons (Google left / Apple right) on tap, then commits on a second tap. Tests the split-open then select interaction.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/split-button"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/split-button--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "split-button"
- waitForAnimationToEnd:
    timeout: 1500
- takeScreenshot: ".maestro/artifacts/split-button--split-open"
- tapOn:
    id: "split-button"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/split-button--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
