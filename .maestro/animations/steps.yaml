# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# steps: 3-step onboarding with animated dots and Back/Continue buttons; the Continue button advances the active step (label becomes 'Finish' on the last). Test: tap Continue repeatedly to advance steps, then Back.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/steps"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/steps--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    text: "Continue"
    optional: true
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    text: "Continue"
    optional: true
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    text: "Back"
    optional: true
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/steps--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
