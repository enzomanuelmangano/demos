# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# github-terrain: WebGPU GitHub-contributions terrain; tapping the full-screen canvas toggles between flat and raised 3D terrain, and a bottom-right Switch toggles real vs demo data.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/github-terrain"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-terrain--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "github-terrain-canvas"
- waitForAnimationToEnd:
    timeout: 2500
- tapOn:
    id: "github-terrain-switch"
- waitForAnimationToEnd:
    timeout: 2500
- tapOn:
    id: "github-terrain-canvas"
- waitForAnimationToEnd:
    timeout: 2500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-terrain--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
