# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# github-terrain: WebGPU 3D terrain built from GitHub contribution data; tapping the full-screen canvas toggles flat<->raised terrain, and a bottom-right Switch toggles real vs sample data. Interaction tested: tap canvas to raise terrain, then flip the data switch.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/github-terrain"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-terrain--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,45%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/github-terrain--raised"
- tapOn:
    id: "Toggle real GitHub contribution data"
    optional: true
- tapOn:
    point: "92%,93%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-terrain--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
