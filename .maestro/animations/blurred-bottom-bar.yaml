# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# blurred-bottom-bar: A bottom-tab navigator (Home / Explore / Camera / Settings) with a blurred glass tab bar and an animated active indicator over full-screen images. Tests tapping tabs to move the indicator.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/blurred-bottom-bar"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/blurred-bottom-bar--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "blurred-bottom-bar-tab-explore"
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    id: "blurred-bottom-bar-tab-camera"
- waitForAnimationToEnd:
    timeout: 1500
- tapOn:
    id: "blurred-bottom-bar-tab-settings"
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "tab-Settings"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/blurred-bottom-bar--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
