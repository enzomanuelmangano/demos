# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# skia-color-picker: A full-width circular Skia color wheel with a draggable picker knob; dragging across the wheel updates the selected color. Tests dragging the knob across the picker area.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/skia-color-picker"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/skia-color-picker--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "30%,40%"
    end: "70%,60%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "70%,60%"
    end: "45%,45%"
- waitForAnimationToEnd:
    timeout: 1500
- takeScreenshot: ".maestro/artifacts/skia-color-picker--picked"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/skia-color-picker--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
