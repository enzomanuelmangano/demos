# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# fourier-visualizer: Full-screen Skia drawing canvas: the user draws a freehand path with a pan gesture, then on release the Fourier series animates epicycles redrawing that path. Tests drawing a stroke across the canvas.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/fourier-visualizer"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/fourier-visualizer--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "30%,40%"
    end: "70%,60%"
- swipe:
    start: "70%,60%"
    end: "40%,70%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/fourier-visualizer--drawn"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/fourier-visualizer--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
