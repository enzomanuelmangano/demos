# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# github-contributions: GitHub-style contribution heatmap that plays a self-running wave animation across the squares with no required interaction. [passive — no user input; verified it renders + survives a tap]
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/github-contributions"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-contributions--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "ready"
    timeout: 4000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/github-contributions--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
