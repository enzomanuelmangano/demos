# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# color-carousel: Full-width paged color carousel backed by a radial-gradient Skia canvas; the active index drives the background color. Swipe horizontally to page through items.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/color-carousel"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/color-carousel--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "color-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 2000
- swipe:
    from:
      id: "color-carousel"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/color-carousel--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
