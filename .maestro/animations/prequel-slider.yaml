# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# prequel-slider: Skia image editor whose ButterflyWaveScrawler GL transition progress is driven by a horizontal ruler/scrubber slider near the bottom of the screen. Tests dragging the bottom scrubber left and right to scrub the transition.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/prequel-slider"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/prequel-slider--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "prequel-slider-scrubber"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 1800
- swipe:
    from:
      id: "prequel-slider-scrubber"
    direction: "RIGHT"
- waitForAnimationToEnd:
    timeout: 1800
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/prequel-slider--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
