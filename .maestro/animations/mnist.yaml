# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# mnist: Neural-network MNIST demo: bottom half is a 28x28 Skia drawing canvas with an eraser button. Draw a digit with a couple of strokes to drive live prediction.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/mnist"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/mnist--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "mnist-canvas"
    direction: "RIGHT"
- waitForAnimationToEnd:
    timeout: 800
- swipe:
    from:
      id: "mnist-canvas"
    direction: "DOWN"
- waitForAnimationToEnd:
    timeout: 800
- swipe:
    from:
      id: "mnist-canvas"
    direction: "LEFT"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 1500
- assertVisible:
    text: "drawn"
    timeout: 4000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/mnist--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
