# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# loading-button: A status-morphing button ('Check Transaction') that animates idle -> loading -> success/error with color and label transitions. Tests tapping to run the 2s mutation and observe the state change.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/loading-button"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/loading-button--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    id: "loading-button"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/loading-button--result"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/loading-button--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
