# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# stacked-carousel: A 3D stacked card carousel with a paginator; horizontal scroll snaps through cards while stacked cards behind translate/scale. Tests advancing the stack with horizontal swipes and swiping back.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/stacked-carousel"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/stacked-carousel--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    start: "80%,45%"
    end: "20%,45%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "80%,45%"
    end: "20%,45%"
- waitForAnimationToEnd:
    timeout: 1500
- swipe:
    start: "20%,45%"
    end: "80%,45%"
- waitForAnimationToEnd:
    timeout: 1500
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/stacked-carousel--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
