# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# ios-home-grid: An iOS home screen 4-column app grid; tapping an icon runs a shared-element expansion transition into a full-screen app detail. Tests tapping an icon to expand into detail then returning.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/ios-home-grid"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/ios-home-grid--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "20%,18%"
- waitForAnimationToEnd:
    timeout: 2000
- takeScreenshot: ".maestro/artifacts/ios-home-grid--detail"
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 2000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/ios-home-grid--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
