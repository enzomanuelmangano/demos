# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# art-gallery: WebGPU photo-mosaic that reconstructs paintings from thousands of tiny photos; double-tap zooms into the tapped cell (grid mode), pinch/pan navigate, double-tap again zooms out. Interaction tested: double-tap to zoom into a cell, then double-tap to zoom back out.
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/art-gallery"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/art-gallery--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- tapOn:
    point: "50%,50%"
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: ".maestro/artifacts/art-gallery--zoomed"
- tapOn:
    point: "50%,50%"
- tapOn:
    point: "50%,50%"
- waitForAnimationToEnd:
    timeout: 3000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/art-gallery--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
