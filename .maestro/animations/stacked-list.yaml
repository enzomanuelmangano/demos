# AUTO-GENERATED by scripts/generate-e2e.mjs from scripts/e2e-specs.json — do not edit by hand.
# stacked-list: A long vertical scroll list (50 cards) where items scale and fade into a stacked deck at the bottom as you scroll. Performs several up-swipes, then ASSERTS the list actually scrolled (stacked-list-status reads "moved", bridged from the worklet scrollOffset value).
appId: com.reactiive.app
---
- launchApp
- openLink: "demos://animations/stacked-list"
# prove the demo actually mounted (not the "Animation … not found" screen)
- extendedWaitUntil:
    notVisible: "not found"
    timeout: 8000
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/stacked-list--mounted"
# --- the demo's own interaction (real HID touches; Skia hit-tests the point) ---
- swipe:
    from:
      id: "stacked-list-list"
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1000
- swipe:
    from:
      id: "stacked-list-list"
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1000
- swipe:
    from:
      id: "stacked-list-list"
    direction: "UP"
- waitForAnimationToEnd:
    timeout: 1000
- assertVisible:
    text: "moved"
- waitForAnimationToEnd:
    timeout: 4000
- takeScreenshot: ".maestro/artifacts/stacked-list--after"
# still on the demo (didn't bounce to not-found) and not crashed
- assertNotVisible: "not found"
